<%= "#{@rtag.name}一覧" %>              #クリックしてもらったタグの名前を一番上に表示しています。
<br>
<% @ritirans.each do |t| %>
    <%= link_to t.body, t %>               #index.html.erbみたいに持ってきた情報をtに代入してreviewsテーブルのbodyカラムを表示します。
    <br>
    <% t.rtags.each do |tag| %>
    <%= link_to tag.name, search_reviews_path(rtag_id: rtag.id) %>
    <% end %>
<% end %>
