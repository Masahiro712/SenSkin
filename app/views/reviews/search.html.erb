<%= "#{@rtag.name}一覧" %>             
<br>
<% @ritirans.each do |t| %>
    <%= link_to t.item, t %>               
    <br>
    <% t.rtags.each do |rtag| %>
    <%= link_to rtag.name, rtag_reviews_path(rtag_id: rtag.id) %>
    <% end %>
<% end %>
