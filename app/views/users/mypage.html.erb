<h1>マイページ</h1>
<%= image_tag(@user.image.present? ? @user.image.url : "アイコン_unknown.png", style: "max-width: 250px; height: auto;") %>
<h3>自己紹介</h3>
<p><%= @user.selfintroduction %></p>
<% if current_user == @user %>
  <p><%= link_to "プロフィール編集", edit_user_path(@user.id) %></p>
<% end %>